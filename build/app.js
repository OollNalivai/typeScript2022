"use strict";
const user = {
    name: 'Pol',
    roles: [],
    permission: {
        endDate: new Date()
    }
};
const nameUser = user['name'];
let roleNames = 'roles';
const roles = ['admin', 'user', 'super-user'];
